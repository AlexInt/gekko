# 模拟交易（Paper Trading）

## 目标

在不触达真实交易所的前提下，复用下单/成交/资产变更逻辑，用于：

- 开发期联调与 UI 验证
- 策略灰度、风控验证

## 关键点

- 后端下单入口会携带 `is_paper=true`
- 成交价格通常使用“当下行情”或“最近一根 K 线价格”的近似

## 现状说明

当前 `OrderManager` 对 `is_paper=false` 的路径尚未实现（实盘未开启）。

